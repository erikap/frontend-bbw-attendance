{{#unless this.loadData.isRunning}}
  <span class="inline-flex items-center px-3 py-0.5 rounded-full text-sm font-medium bg-primary-100 text-primary-800">
    {{this.presentCount}} / {{this.totalCount}}
  </span>
{{/unless}}